# 使用说明

完整使用方法见 [README.md](README.md)，包括安装、更新、手动运行、登录启动、进程与日志核验、重连时间、可选探测、已知限制和卸载。

四个 `.ps1` 文件保持在同一目录，然后在该目录打开 PowerShell 执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\Install-UniVPN-Reconnect.ps1
```

安装副本位于 `%LOCALAPPDATA%\UniVPN-Reconnect`。安装后按 README 核验进程与新日志，不要只看安装完成提示：当前版本仍有历史守护启动失败问题，探测参数也未在所有启动入口保持一致。
